# These run on Linux. CRLF in a shell script produces
# "/usr/bin/env: bash\r: No such file or directory".
* text=auto eol=lf
*.sh text eol=lf

# Never run these through line-ending conversion
*.jpg binary
*.png binary
